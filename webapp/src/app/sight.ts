export class Sight {
    id: number;
    name: string;
    openingHours: string;

    constructor(name: string, openingHours: string) {
      this.name = name;
      this.openingHours = openingHours;
    }
}
